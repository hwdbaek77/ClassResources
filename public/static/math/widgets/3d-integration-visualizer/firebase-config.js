// Optional Firebase config stub to avoid 404s in environments without Firebase.
// Provide your Firebase configuration here if syncing is required.
// Example:
// const firebaseConfig = { /* ... */ };
// if (window.firebase && firebase.initializeApp) { firebase.initializeApp(firebaseConfig); }


